# Release tools only; install after freezing the detector.
-r build-requirements.txt
cryptography==50.0.1
dmgbuild==1.6.7; sys_platform == 'darwin'
ds-store==1.3.3; sys_platform == 'darwin'
mac-alias==2.2.3; sys_platform == 'darwin'
